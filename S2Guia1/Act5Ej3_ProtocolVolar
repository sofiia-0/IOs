import Foundation

// Definición del protocolo Volable
protocol Volable {
    func volar()
}

// Clase Pajaro que conforma al protocolo Volable
class Pajaro: Volable {
    func volar() {
        print("El pájaro está volando.")
    }
}

// Clase Avion que conforma al protocolo Volable
class Avion: Volable {
    func volar() {
        print("El avión está despegando.")
    }
}

// Crear instancias de Pajaro y Avion
let miPajaro = Pajaro()
let miAvion = Avion()

// Llamar al método volar de cada instancia
miPajaro.volar()
miAvion.volar()
